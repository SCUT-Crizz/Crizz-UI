import { withInstall } from '@crizz-ui/utils'
import Button from './Button.vue'
import ButtonGroup from './ButtonGroup.vue'

export const CrButton = withInstall(Button)
export const CrButtonGroup = withInstall(ButtonGroup)

export * from './types'